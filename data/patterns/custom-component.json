{
  "name": "Custom Component",
  "description": "Reusable ScriptComponent that can be attached to any entity",
  "tags": ["component", "reusable", "entity", "behavior"],
  "scripts": [
    {
      "className": "{PREFIX}_CustomComponent",
      "scriptType": "component",
      "parentClass": "ScriptComponent",
      "methods": [],
      "description": "Reusable custom component"
    }
  ],
  "prefabs": [],
  "configs": [],
  "instructions": "CRITICAL: Use api_search to verify EVERY method you call. Never guess Enfusion API method names — they are non-standard and many obvious-sounding methods don't exist.\n1. Use api_search to look up ScriptComponent and any classes your component interacts with — verify methods exist before using them\n2. Edit the component script to add your member variables and logic — only call methods confirmed by api_search\n3. Use [Attribute()] to expose configuration in Workbench\n4. Override EOnInit for initialization, OnPostInit for post-setup — verify these exist on your parent class via api_search\n5. Override OnDelete for cleanup\n6. Attach the component to entity prefabs via their 'components' block\n7. Access the component at runtime: entity.FindComponent(YourComponent)",
  "codeExamples": [
    {
      "title": "Cache a sibling component and react to an event",
      "description": "SCR_DamageManagerComponent.GetDamageManager(owner) is a verified static factory (api_search), and ScriptInvokerBase.Insert(fn) is a verified method for subscribing to a ScriptInvoker such as GetOnDamage(). This avoids the unverifiable IEntity.FindComponent guess by using the confirmed static lookup instead.",
      "code": "protected SCR_DamageManagerComponent m_DamageManager;\n\noverride void EOnInit(IEntity owner)\n{\n  m_DamageManager = SCR_DamageManagerComponent.GetDamageManager(owner);\n  if (m_DamageManager)\n    m_DamageManager.GetOnDamage().Insert(OnDamageTaken);\n}\n\nprotected void OnDamageTaken()\n{\n  // React to damage here, e.g. trigger an effect or update state\n}"
    }
  ]
}
