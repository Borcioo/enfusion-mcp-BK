# Faza 2: „Oczy" — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dać agentowi wgląd w to, co Workbench faktycznie robi: logi + błędy kompilacji (`wb_log`), stan świata w play mode (`wb_game_state`), zrzut viewportu (`wb_screenshot`), oraz naprawić pak-reader padający na plikach generated.

**Architecture:** `wb_log` czyta pliki logów Workbencha z dysku (deterministyczne, zero zmian w handlerach `.c`) — katalog logów jest znany z configu i wzorca `logs_<timestamp>`; parser błędów kompilacji zamienia linie `SCRIPT (E): @"plik,linia": msg` na strukturę z kontekstem kodu; `wb_reload` po przeładowaniu dołącza świeże błędy. `wb_game_state` to nowy handler Enforce Script czytający świat przez `GetGame()` w play mode (read-only) — z jawnym krokiem badawczym i planem B (debug-print + `wb_log`). `wb_screenshot` ma trzy ścieżki (akcja menu / API skryptowe / zrzut okna z Node) z jawnym kryterium rezygnacji.

**Tech Stack:** TypeScript (ESM, Node >= 20), Vitest; Enforce Script tylko w Task 4 (nowy handler).

## Global Constraints

- Repo: `D:\Projekty\ArmaReforger\enfusion-mcp-BK`, branch bazowy `main` (po merge PR #2).
- Kod, komentarze, commity: **angielski**; konwencja `feat:`/`fix:`/`test:`/`docs:`.
- Każdy task = osobna gałąź + PR do `Borcioo/enfusion-mcp-BK:main`; przed commitem `npm run build && npm test` zielone.
- Checklist repo: testy, `description` narzędzi, prompty (`create-mod`/`modify-mod`) jeśli dotyczy, tabela README, wpis w `UPGRADE_IDEAS.md` (#16 → done po Task 1-2).
- Nowe narzędzia rejestrowane w `src/server.ts`.
- Live testy na Workbench + Central-Economy (world `Worlds/Testing/Testing.ent`); po testach revert `git checkout source/Worlds/` w repo Central-Economy; NIE commitować w repo CE.
- Katalog logów WB na tej maszynie: `C:\Users\macie\OneDrive\Dokumenty\My Games\ArmaReforgerWorkbench\logs\logs_<YYYY-MM-DD_HH-MM-SS>\` (nie hardkodować ścieżki użytkownika w kodzie — wyprowadzać z `%USERPROFILE%`/profilu; hardkod tylko w configu testowym).

---

### Task 1: `wb_log` — czytanie logów Workbencha z parserem błędów kompilacji

**Files:**
- Create: `src/workbench/logs.ts` (lokalizacja katalogu logów + tail + parser)
- Create: `src/tools/wb-log.ts` (narzędzie MCP)
- Modify: `src/server.ts` (rejestracja), `src/config.ts` (opcjonalny `workbenchProfileDir`)
- Test: `tests/workbench/logs.test.ts`
- Create: `tests/fixtures/console-sample.log` (fixture z realnych linii — patrz Step 1)

**Interfaces:**
- Produces:
  - `findLatestLogDir(baseDir: string): string | null` — najnowszy `logs_*` po nazwie (timestamp w nazwie sortuje leksykograficznie),
  - `readLogTail(logDir: string, opts: { lines?: number; filter?: RegExp; sinceByte?: number }): { text: string; endByte: number }`,
  - `parseCompileErrors(text: string): CompileError[]` gdzie `CompileError = { file: string; line: number; message: string }`,
  - narzędzie `wb_log` (params: `lines` default 100, `filter` regex string opt., `errorsOnly` bool default false, `file` enum `console|script` default console).

- [ ] **Step 1: Fixture z realnych linii logu**

Utwórz `tests/fixtures/console-sample.log` z ZANOTOWANYCH w tej sesji realnych linii (formaty potwierdzone empirycznie):
```
  SCRIPT       : Compiling Game scripts
   SCRIPT    (E): @"Scripts/Game/CentralEconomy/Components/CE_ItemSpawnableComponentSerializer.c,10": Overloading event 'Serialize' is not allowed
   SCRIPT    (E): @"Scripts/Game/CentralEconomy/Systems/CE_ItemSpawningSystemSerializer.c,31": Overloading event 'Serialize' is not allowed
   SCRIPT    (W): @"Scripts/Game/Network/ServerCommands/BanCommands.c,63": 'OnError' is obsolete: Use BackendCallback.SetOnError() instead.
   SCRIPT    (E): Can't compile "Game" script module!
NETWORK   (E): Failed to call not existing Net API function 'EMCP_WB_Ping'
    RESOURCES (E): Wrong GUID/name for resource @"{0000000000000000}$profile:/CentralEconomy/CE_ItemData.conf" in property "m_sDb"
    SCRIPT    (E): [CentralEconomy::CE_WorldValidationComponent] CE_ItemData.conf is empty! This is located in your server's profile folder, please resolve!
GUI       (E): OverlayWidget Frame0 has invalid slot GridWidgetSlot for slot manipulation function used, expected: LayoutSlot
```

- [ ] **Step 2: Failing testy parsera i tail-a**

`tests/workbench/logs.test.ts`:
```typescript
import { readFileSync, mkdirSync, writeFileSync, rmSync } from "node:fs";
import { join } from "node:path";
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { findLatestLogDir, readLogTail, parseCompileErrors } from "../../src/workbench/logs.js";

const SAMPLE = readFileSync("tests/fixtures/console-sample.log", "utf-8");

describe("parseCompileErrors", () => {
  it("extracts file, line and message from SCRIPT (E) lines", () => {
    const errors = parseCompileErrors(SAMPLE);
    expect(errors[0]).toEqual({
      file: "Scripts/Game/CentralEconomy/Components/CE_ItemSpawnableComponentSerializer.c",
      line: 10,
      message: "Overloading event 'Serialize' is not allowed",
    });
    // only @"file,line" script errors count as compile errors
    expect(errors.every(e => e.file.endsWith(".c"))).toBe(true);
    expect(errors).toHaveLength(2);
  });

  it("ignores warnings and non-script errors", () => {
    const errors = parseCompileErrors(SAMPLE);
    expect(errors.some(e => e.message.includes("obsolete"))).toBe(false);
    expect(errors.some(e => e.message.includes("GridWidgetSlot"))).toBe(false);
  });
});

describe("findLatestLogDir", () => {
  const base = "tests/.tmp-logs";
  beforeAll(() => {
    mkdirSync(join(base, "logs_2026-07-01_10-00-00"), { recursive: true });
    mkdirSync(join(base, "logs_2026-07-03_00-56-02"), { recursive: true });
    mkdirSync(join(base, "not-a-log-dir"), { recursive: true });
  });
  afterAll(() => rmSync(base, { recursive: true, force: true }));

  it("returns the lexicographically newest logs_* dir", () => {
    expect(findLatestLogDir(base)).toContain("logs_2026-07-03_00-56-02");
  });
  it("returns null when no logs_* dirs exist", () => {
    expect(findLatestLogDir(join(base, "not-a-log-dir"))).toBeNull();
  });
});

describe("readLogTail", () => {
  const base = "tests/.tmp-tail";
  beforeAll(() => {
    mkdirSync(base, { recursive: true });
    writeFileSync(join(base, "console.log"), SAMPLE);
  });
  afterAll(() => rmSync(base, { recursive: true, force: true }));

  it("returns last N lines and an endByte cursor for incremental reads", () => {
    const r1 = readLogTail(base, { lines: 3 });
    expect(r1.text.split("\n").filter(Boolean)).toHaveLength(3);
    expect(r1.endByte).toBeGreaterThan(0);
    const r2 = readLogTail(base, { sinceByte: r1.endByte });
    expect(r2.text).toBe("");
  });

  it("applies a filter regex", () => {
    const r = readLogTail(base, { filter: /SCRIPT\s+\(E\)/ });
    expect(r.text.split("\n").filter(Boolean).length).toBeGreaterThanOrEqual(3);
    expect(r.text).not.toMatch(/GUI/);
  });
});
```

- [ ] **Step 3: Run — verify FAIL** (`npx vitest run tests/workbench/logs.test.ts` → moduł nie istnieje)

- [ ] **Step 4: Implementacja `src/workbench/logs.ts`**

```typescript
/**
 * Workbench log access: locate the newest logs_<timestamp> directory,
 * tail console.log incrementally, and parse script compile errors.
 *
 * File-based on purpose: reading logs from disk works even when the NET API
 * handlers failed to compile — which is exactly when you need the logs most
 * (observed live: a broken Game module blocks all EMCP_* handlers).
 */
import { readdirSync, statSync, openSync, readSync, closeSync } from "node:fs";
import { join } from "node:path";

export interface CompileError {
  file: string;
  line: number;
  message: string;
}

/** SCRIPT (E): @"path/file.c,123": message */
const COMPILE_ERROR_RE = /SCRIPT\s+\(E\):\s+@"([^",]+\.c),(\d+)":\s+(.+)$/;

export function findLatestLogDir(baseDir: string): string | null {
  let dirs: string[];
  try {
    dirs = readdirSync(baseDir).filter(d => d.startsWith("logs_"));
  } catch {
    return null;
  }
  if (dirs.length === 0) return null;
  dirs.sort(); // timestamp format sorts lexicographically
  return join(baseDir, dirs[dirs.length - 1]!);
}

export function parseCompileErrors(text: string): CompileError[] {
  const errors: CompileError[] = [];
  for (const line of text.split("\n")) {
    const m = COMPILE_ERROR_RE.exec(line);
    if (m) errors.push({ file: m[1]!, line: parseInt(m[2]!, 10), message: m[3]!.trim() });
  }
  return errors;
}

export function readLogTail(
  logDir: string,
  opts: { lines?: number; filter?: RegExp; sinceByte?: number; fileName?: string } = {},
): { text: string; endByte: number } {
  const filePath = join(logDir, opts.fileName ?? "console.log");
  let size: number;
  try {
    size = statSync(filePath).size;
  } catch {
    return { text: "", endByte: 0 };
  }
  const start = Math.max(opts.sinceByte ?? 0, 0);
  if (start >= size) return { text: "", endByte: size };

  // Read at most the last 1 MB when no cursor was given (protects against huge logs)
  const MAX_READ = 1024 * 1024;
  const from = opts.sinceByte !== undefined ? start : Math.max(start, size - MAX_READ);
  const fd = openSync(filePath, "r");
  const buf = Buffer.alloc(size - from);
  try {
    readSync(fd, buf, 0, buf.length, from);
  } finally {
    closeSync(fd);
  }
  let lines = buf.toString("utf-8").split("\n");
  if (opts.filter) lines = lines.filter(l => opts.filter!.test(l));
  if (opts.lines !== undefined) lines = lines.filter(Boolean).slice(-opts.lines);
  return { text: lines.join("\n"), endByte: size };
}
```
Dokładne nazwy plików logów zweryfikuj na żywym katalogu (Step 6) — jeśli konsola nazywa się inaczej niż `console.log`, popraw default i test.

- [ ] **Step 5: Narzędzie `wb_log` + rejestracja**

`src/tools/wb-log.ts`: schema `{ lines: z.number().min(1).max(1000).default(100), filter: z.string().optional(), errorsOnly: z.boolean().default(false) }`. Lokalizacja bazy logów: `join(homedir(), "Documents", "My Games", "ArmaReforgerWorkbench", "logs")` z override przez `config.workbenchProfileDir` (nowe opcjonalne pole w `Config` — na tej maszynie Documents jest pod OneDrive, więc dodaj też próbę `join(homedir(), "OneDrive", "Dokumenty", ...)` i `join(homedir(), "OneDrive", "Documents", ...)`; wybierz pierwszy istniejący, zaloguj który). Przy `errorsOnly` zwróć wynik `parseCompileErrors` + dla każdego błędu ±5 linii kontekstu z pliku źródłowego (szukaj pliku względem `config.projectPath/<defaultMod>` oraz katalogów addonów). Opisz w description: „Read Workbench console logs from disk. Works even when NET API handlers fail to compile."

- [ ] **Step 6: Run all + live check**

`npm run build && npm test` → PASS. Live: wywołaj `wb_log` (errorsOnly=false, lines=20) przy działającym WB → zwraca świeże linie; zepsuj celowo składnię w dowolnym pliku `.c` Central-Economy, w WB wciśnij reload skryptów, `wb_log errorsOnly=true` → zwraca błąd z plikiem/linią/kontekstem; cofnij psucie.

- [ ] **Step 7: Commit + PR** (`feat/wb-log`) — commit message: `feat: add wb_log tool reading Workbench console logs with compile-error parsing`. Odhacz część #16 w UPGRADE_IDEAS.md (log capture; error-feedback dokończy Task 2).

### Task 2: `wb_reload` z automatycznym feedbackiem błędów kompilacji

**Files:**
- Modify: `src/tools/wb-reload.ts`
- Test: `tests/tools/wb-reload.test.ts` (nowy)

**Interfaces:**
- Consumes: `findLatestLogDir`, `readLogTail`, `parseCompileErrors` z `src/workbench/logs.ts` (Task 1).
- Produces: `wb_reload` zwracający sekcję `**Compile Errors**` gdy po reloadzie w logu pojawiły się `SCRIPT (E)` z plikiem/linią.

- [ ] **Step 1: Failing test** — wydziel z `wb-reload.ts` czystą funkcję `collectNewCompileErrors(logDir, beforeByte): CompileError[]` (odczyt od kursora sprzed reloadu, parse); test na katalogu tymczasowym: zapisz plik logu bez błędów → pusta lista; dopisz linie z błędem → zwraca 1 błąd (użyj linii z fixture Task 1).
- [ ] **Step 2: FAIL → implementacja** — w handlerze narzędzia: przed `client.call(reload)` pobierz `endByte` aktualnego logu; po wywołaniu odczekaj krótko na dopis (poll do ~5 s co 500 ms aż `endByte` uróśnie lub timeout) i sparsuj nowe linie; błędy dołącz do odpowiedzi z kontekstem kodu (reuse formatowania z Task 1 Step 5).
- [ ] **Step 3: PASS → live test** — celowo zepsuty skrypt + `wb_reload` przez MCP → odpowiedź zawiera plik:linię bez pytania użytkownika o konsolę. Cofnij psucie.
- [ ] **Step 4: Commit + PR** (`feat/reload-error-feedback`); domknij #16 w UPGRADE_IDEAS.md (`✅ Done (PR #N)`).

### Task 3: `wb_game_state` — inspekcja świata w play mode

**Files:**
- Create: `mod/Scripts/WorkbenchGame/EnfusionMCP/EMCP_WB_GameState.c`
- Create: `src/tools/wb-game-state.ts`; Modify: `src/server.ts`
- Test: brak unit (handler `.c`); live checklist w raporcie

**Interfaces:**
- Produces: narzędzie `wb_game_state` z akcjami `world_info` (czas, liczba encji), `list_entities` (filtr po substringu nazwy prefabu/klasy, limit+offset, pozycje), `players` (lista graczy + pozycje).

- [ ] **Step 1: RESEARCH — dostęp do świata gry z handlera NET API w play mode**

Przeczytaj `mod/Scripts/WorkbenchGame/EnfusionMCP/EMCP_WB_GetState.c` (wzorzec handlera i wykrywanie play mode). Sprawdź przez `api_search`: `GetGame`, `ChimeraWorld`/`BaseWorld` (iteracja encji — szukaj `QueryEntitiesBySphere`/`GetEntitiesList`), `PlayerManager` (`GetPlayers`). Napisz minimalny handler zwracający `GetGame() != null` + `GetGame().GetWorld() != null` w play mode; wgraj (kopiuj do moda jak instalator: plik trafia też do `Central-Economy/source/Scripts/WorkbenchGame/EnfusionMCP/`), reload skryptów, `wb_play`, wywołaj handler surowym callem. Wynik determinuje resztę:
  - **Dostęp jest** → Step 2.
  - **Brak dostępu** (null/wyjątek) → udokumentuj w tym pliku planu dokładny objaw i przejdź do Step 2-B (plan B).

- [ ] **Step 2 (ścieżka A): pełny handler** — `world_info`: `world.GetWorldTime()`, liczba encji jeśli API na to pozwala; `list_entities`: iteracja przez query API świata (sfera o dużym promieniu wokół 0 0 0 lub dedykowane API — wg wyniku researchu), filtr po nazwie klasy (`entity.ClassName()`) i prefabu, paginacja jak w `EMCP_WB_ListEntities.c`; `players`: `GetGame().GetPlayerManager()`. WSZYSTKO read-only. Limity: max 200 encji na odpowiedź.
- [ ] **Step 2-B (plan B, tylko gdy A niemożliwe):** komponent debug w pakiecie handlerów (`EMCP_GameStateLogger.c` jako ScriptComponent dokładany do GameMode przez użytkownika w worldzie testowym), wypisujący co N sekund stan do logu w formacie `EMCP_STATE: {json}`; `wb_game_state` czyta przez `readLogTail` + filtr. Mniej wygodne, ale działa.
- [ ] **Step 3: Narzędzie TS** — `wb_game_state` woła handler (lub czyta log w wariancie B); opis jasno mówi, że działa w PLAY mode (odwrotnie niż reszta wb_*).
- [ ] **Step 4: Live E2E** — world Testing + `wb_play`; `wb_game_state world_info` → czas rośnie między wywołaniami; `list_entities` filtr `GameModeSF` → znajduje game mode. Po teście `wb_stop`, revert worlda CE jeśli dotknięty.
- [ ] **Step 5: Commit + PR** (`feat/wb-game-state`).

### Task 4: `wb_screenshot`

**Files:**
- Create: `src/tools/wb-screenshot.ts`; Modify: `src/server.ts`; ewent. nowy handler `.c`
- Test: unit tylko dla logiki ścieżek zapisu; werdykt ścieżki A/B/C w raporcie

- [x] **Step 1: RESEARCH (time-box: nie drąż w nieskończoność)** — kolejno:
  A. `api_search`/`wiki_search`: `MakeScreenshot`, `Screenshot`, `System.` — czy Enforce w kontekście Workbencha ma API zrzutu (jeśli tak: handler `.c` zapisujący do `$profile:` i zwracający ścieżkę).
  B. `wb_execute_action` — przejrzyj menu WB na żywo (user może podejrzeć: Window/Help/Debug) za akcją screenshot.
  C. Fallback Node: PowerShell zrzut okna po tytule procesu (`Add-Type` + `System.Drawing`/PrintWindow) do PNG w scratch dir — działa zawsze, łapie całe okno WB (z menu), nie sam viewport.

  **Werdykt:** A istnieje (`System.static proto bool MakeScreenshot(string path)`, BMP), ale wymagałby *nowego* handlera WorkbenchGame (`EMCP_WB_Screenshot.c`) — handlery w module WorkbenchGame ładują się dopiero po pełnym restarcie Workbencha, więc nie dało się tego zweryfikować na żywo w tej sesji. B nie testowano na żywo (nie churnować działającego WB dla eksploracji menu) — brak w kodzie źródłowym dowodu na istniejącą akcję menu do zrzutu edytora. C wybrano jako pragmatyczną ścieżkę: PowerShell + `PrintWindow` (flaga `PW_RENDERFULLCONTENT`), zero handlerów, zero restartu, w pełni weryfikowalne na żywo od razu.
- [x] **Step 2: Implementacja najlepszej działającej ścieżki** — zaimplementowano C (`src/tools/wb-screenshot.ts`, zarejestrowane w `src/server.ts` jako `wb_screenshot`). Ograniczenie (całe okno WB, nie sam viewport) opisane w description narzędzia i README.
- [x] **Step 3: Live test** — zrzut przy otwartym worldzie CentralEconomy/Testing: PNG 2318×1334, ~2.06 MB, poprawny nagłówek PNG, obraz potwierdzony wizualnie (World Editor, Hierarchy, viewport z zaznaczonymi entity, Log Console) — brak modala Diag.
- [x] **Step 4: Commit + PR** (`feat/wb-screenshot`) — commit utworzony, PR nie tworzony (poza zakresem tego zadania).

### Task 5: Fix pak-readera („invalid block type")

**Files:**
- Modify: `src/pak/*` (reader) — dokładny plik wg diagnozy
- Test: `tests/pak/` — rozszerzenie istniejących

- [ ] **Step 1: Repro** — `game_read` na `scripts/Game/generated/Plugins/Persistence/System/Serializers/ScriptedComponentSerializer.c` → błąd `invalid block type` (potwierdzone w tej sesji). Zbadaj w `src/pak/` jak dekompresowane są wpisy (zlib? store?); wyciągnij surowe bajty wpisu i ustal typ bloku, którego reader nie zna.
- [ ] **Step 2: Failing test** — jeśli da się wyodrębnić mały fragment paka jako fixture (nagłówek + feralny wpis) bez łamania praw — test na nim; jeśli nie, test na sztucznym buforze odtwarzającym typ bloku + live check.
- [ ] **Step 3: Fix + PASS** — obsłuż brakujący typ bloku (lub czytelny błąd z nazwą typu zamiast ogólnego); `game_read` na feralnym pliku zwraca treść (zawiera `class ScriptedComponentSerializer`), co przy okazji da nam źródło prawdy o sygnaturach eventów.
- [ ] **Step 4: Commit + PR** (`fix/pak-block-type`).

---

## Kryteria zamknięcia planu

- `wb_log` + `wb_reload` z błędami kompilacji działają na żywo (koniec ręcznego wklejania konsoli).
- `wb_game_state` działa w play mode (ścieżka A) albo plan B wdrożony z udokumentowanym powodem.
- `wb_screenshot` działa albo rezygnacja udokumentowana w spec.
- Pak-reader czyta plik generated ScriptedComponentSerializer.c.
- UPGRADE_IDEAS #16 domknięty; wszystkie PR-y zmergowane; wyniki dopisane do tego pliku.
- Następny krok: plan Fazy 3 (S-ki: dry-run #10, konsolidacja dir-listing #11).
